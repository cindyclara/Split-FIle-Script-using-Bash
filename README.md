# Split-FIle-Script-using-Bash
Bash script to split file into several files either by number of lines/number of files, zip the split file &amp; send it out to other path &lt;optional>

To use this script:
